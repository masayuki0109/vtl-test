type Todo @model @auth(rules: [{allow: public}]) {
  id: ID!
  name: String!
  description: String
}

input TodoInput {
    id: ID
    name: String!
    description: String
}

type Mutation {
    batchAddTodo(todos: [TodoInput]): [Todo]
    batchDeleteTodo(ids: [ID]): [Todo]
}

